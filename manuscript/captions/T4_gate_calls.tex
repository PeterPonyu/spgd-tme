Native reportability calls across the eight libraries. For each library the malignant--neighbor cosine of Equation~\eqref{eq:native-cosine} is evaluated on the designated pair at interpolation fraction \(t=0\) and compared with the frozen cutoff \(c^\star=0.80\) of Equation~\eqref{eq:keep-gate}. A cosine below the cutoff returns KEEP and a tumor fraction is written; a cosine at or above it returns ABSTAIN and the malignant coordinate is withheld. Four CosMx carcinomas fall below the cutoff and return KEEP, while CosMx PDAC abstains on the same platform that carries all four KEEP calls and the three sweep substrates abstain, so both faces of the gate occur on CosMx and the pattern is consistent with malignant--neighbor collinearity rather than with the assay.
