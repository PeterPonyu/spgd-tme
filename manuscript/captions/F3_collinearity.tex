Collinearity dose--response on three locked substrates (openST, Xenium, CosMx). The horizontal axis is the interpolation fraction that blends the locked malignant signature toward its locked neighbor. The vertical axis is tumor-fraction RMSE with the refusal gate off. Open circles at \(t=0\) are the 400-spot bootstrap mean with a 95\% interval (\(B=1000\)); diamonds are full-\(n\) confirmation. The threshold \(c^\star=0.80\) was locked before the sweep.
