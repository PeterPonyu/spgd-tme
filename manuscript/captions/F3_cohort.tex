Four-patient cell-level fields give a clinically separable tissue. Panels A--D colour every cell by its annotated type, using the same eight-type key the composition panel stacks, and are cell-level geography rather than mixed-spot composition. Grey squares mark recorded fields of view (nominal 0.51 mm); each tile keeps its micrometre extent, so white gutters are native field geometry. (A)~Packed typed-cell field for Patient A. (B)~Packed typed-cell field for Patient B, which has 17 fields and therefore one unused pack slot. (C)~Packed typed-cell field for Patient C. (D)~Packed typed-cell field for Patient D. (E)~Typed-cell composition of all eight shared types for each patient and condition, stacked to \(1\) and coloured by type. Patient A cancer fraction is \(0.4922\) and Patient B is \(0.8277\). On Patient D, cancer falls from \(0.8863\) at Baseline to \(0.5396\) at Wound, while fibroblast rises from \(0.0607\) to \(0.1829\) and MoMacDC from \(0.0057\) to \(0.1266\). These are typed-cell fractions, not mixed-spot pair statistics. (F)~Typed cells per donor for each of the eight shared types, coloured by patient on a logarithmic vertical axis. The dashed line is the builder floor of fifty reference cells per type; all 32 donor-by-type counts clear it, the smallest being 137 Pericytes on Patient B.
