Directed donor transfer recovers composition across patients. (A)~Overall RMSE, tumor RMSE, type PCC, and spot PCC on the four directed edges (computed blue; locked C-from-D orange). B from A is the tightest computed edge (overall RMSE 0.0659, spot-level PCC 0.9528). Four-edge overall RMSE is 0.0659--0.1349 (A from B 0.1349, B from A 0.0659, C from D 0.1149, D from C 0.0906). Type-level PCC is 0.7034--0.7708. Spot-level PCC is 0.8030 on A from B, 0.9528 on B from A, 0.8773 on C from D, and 0.9037 on D from C. (B)~Empirical cumulative distribution of per-spot tumor truth on the three computed edges; the vertical axis is the share of spots at or below a tumor fraction. Each curve's final jump is its share of pure-tumor spots: 13\% on A from B, 49\% on B from A, and 35\% on D from C. A from B is the most mixed edge and B from A the most nearly pure. (C)~Mean spot occupancy per computed edge, stacked over the three tracked types and the residual five. Cancer occupancy is 0.403 on A from B, 0.776 on B from A, and 0.650 on D from C; the residual five types hold 0.253, 0.085, and 0.135. (D)~B-from-A mixed-spot tumor truth on packed Patient B fields (2020 spots). Point colour is the per-spot Cancer.cells truth fraction on [0,1] (cell-count share). (E)~D-from-C mixed-spot tumor truth on packed Patient D fields (3261 spots). Point colour is the per-spot Cancer.cells truth fraction on [0,1]. (F)~A-from-B mixed-spot tumor truth on packed Patient A fields (1194 spots). Point colour is the per-spot Cancer.cells truth fraction on [0,1]. C-from-D composition closes the four-edge matrix.
