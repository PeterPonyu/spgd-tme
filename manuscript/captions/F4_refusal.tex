Refusal overlay on the collinearity sweep. Grey traces are ungated tumor RMSE. Open green points are retained gate-on RMSE where the locked cosine stays below \(c^\star=0.80\). Shaded bands mark ABSTAIN settings, where composition continues on the remaining types.
