One frozen cosine produces a platform-dependent KEEP window. (A)~Ungated tumor RMSE versus interpolation fraction \(t\) on openST, as the malignant signature is blended toward its neighbor. The vertical axis is ungated tumor RMSE. At \(t=0\), the 400-spot mean is \(0.0757\) (95\% bootstrap CI \(0.0643\)--\(0.0874\); \(B=1000\)); full-\(n\) confirmation is \(0.0903\) (\(n=6971\)). (B)~Ungated tumor RMSE versus \(t\) on Xenium. The vertical axis is ungated tumor RMSE. At \(t=0\), the 400-spot mean is \(0.1558\) (\(0.1380\)--\(0.1724\)); full-\(n\) confirmation is \(0.1613\) (\(n=2864\)). (C)~Ungated tumor RMSE versus \(t\) on CosMx. The vertical axis is ungated tumor RMSE. At \(t=0\), the 400-spot mean is \(0.1542\) (\(0.1364\)--\(0.1729\)); full-\(n\) confirmation is \(0.1516\) (\(n=5686\)). Across the CosMx sweep, ungated RMSE rises from \(0.1542\) to \(0.6862\). (D)~Tumor RMSE versus \(t\) on openST with a KEEP/ABSTAIN overlay: grey is ungated RMSE; open green marks KEEP settings where the locked cosine stays below \(c^\star=0.80\). Across the openST sweep, RMSE rises from \(0.0757\) to \(0.3357\). (E)~Tumor RMSE versus \(t\) on Xenium with a KEEP/ABSTAIN overlay: grey is ungated RMSE; open green marks KEEP settings where the locked cosine stays below \(c^\star=0.80\). (F)~Tumor RMSE versus \(t\) on CosMx with a KEEP/ABSTAIN overlay: grey is ungated RMSE; open green marks KEEP settings where the locked cosine stays below \(c^\star=0.80\). CosMx remains KEEP at \(t=0,0.1,0.2\) and writes ABSTAIN from \(t=0.3\).
