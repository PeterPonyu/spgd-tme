The frozen cutoff \(c^\star=0.80\) tracks malignant--neighbor collinearity as it is dialled up: each substrate holds KEEP until its interpolated cosine crosses the cutoff. (A)~Ungated tumor RMSE versus interpolation fraction \(t\) on openST, as the malignant signature is blended toward its neighbor. The error bar at \(t=0\) is the 95\% bootstrap interval \(0.0643\)--\(0.0874\) (\(B=1000\)) around the 400-spot mean \(0.0757\), and the open circle is the full-\(n\) confirmation \(0.0903\) (\(n=6971\)). Shading marks the settings whose interpolated cosine is at or above the cutoff, where the gate writes ABSTAIN; openST enters the sweep above the cutoff, so the whole panel is shaded. (B)~The same sweep on Xenium. The 400-spot mean at \(t=0\) is \(0.1558\) (\(0.1380\)--\(0.1724\)) and full-\(n\) confirmation is \(0.1613\) (\(n=2864\)); Xenium also enters above the cutoff. (C)~The same sweep on CosMx. The 400-spot mean at \(t=0\) is \(0.1542\) (\(0.1364\)--\(0.1729\)) and full-\(n\) confirmation is \(0.1516\) (\(n=5686\)); ungated RMSE rises from \(0.1542\) to \(0.6862\) across the sweep, and shading begins at \(t=0.3\). (D)~The openST sweep read in the cutoff's own coordinate: tumor RMSE against the malignant--neighbor cosine, with the arrow running from the native setting (open diamond, \(t=0\)) to \(t=1\), points coloured by the call at that setting, and the dashed line and shading at \(c^\star=0.80\). openST enters at \(0.9727\) and refuses throughout. (E)~The same reading on Xenium, which enters at \(0.9802\) and refuses throughout. (F)~The same reading on CosMx, which enters at \(0.6037\). Tumor RMSE climbs monotonically with the cosine; the last KEEP setting is \(t=0.2\) and the sweep crosses the cutoff at \(t=0.3\).
