Full-library and full-sample consistency checks. (A) Historical designated-pair cosine versus the complete eligible-neighbor maximum for all eight libraries; the frozen \(c^\star=0.80\) line is unchanged. (B) Fresh matched 800-spot comparison of SPGD and Tangram on CosMx BCC using the same locked cell-count truth; for RMSE and JSD lower is better, for PCC higher is better. (C) Full-\(n\) comparison of the pre-gate reference profile \(P\) and fit signature \(S\); the openST discrepancy persists at full scale, whereas the other three libraries give the same decision. (D) Full-\(n\) BCC consequence of applying the maximum-eligible sensitivity rule: the malignant coordinate is withheld, and the remaining ten types are scored after renormalization. That conditional RMSE is a different estimand from the historical eleven-type full-simplex score (\(0.106\)). Spots with no non-malignant truth mass are excluded and counted (\(1{,}553\) of \(5{,}686\)); the conditional RMSE on the remaining \(4{,}133\) is \(0.188\). Scoring the zero-mass rows as an all-zero composition yields \(0.214\), which is superseded. The two RMSEs do not rank the designated pair against the maximum.
