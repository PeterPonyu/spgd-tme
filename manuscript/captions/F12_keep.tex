The same frozen cosine reports KEEP on independent CosMx carcinomas. (A)~Locked malignant--neighbor cosine versus \(c^\star=0.80\) on CosMx BCC (\(0.6037\), KEEP), NSCLC (\(0.6482\), KEEP), CRC (\(0.5058\), KEEP), HCC (\(0.6486\), KEEP), CosMx PDAC (\(0.8616\), ABSTAIN), openST (\(0.9727\), ABSTAIN), Xenium (\(0.9802\), ABSTAIN), and Xenium FLEX (\(0.9770\), ABSTAIN). The vertical axis is cosine; KEEP is below the cutoff and ABSTAIN is at or above it. (B)~The malignant program of each independent library scored against the stored neighbor rows of the reference export, one point per row, coloured by the call that pair alone would give. The open diamond is the designated stromal pair whose cosine is reported, and shading marks cosines at or above \(c^\star=0.80\). (C)~Overall RMSE on directed Patient\_ID edges, including the NSCLC edge (\(0.1066\) on 4237 spots). (D)~Spot-level Pearson correlation on directed Patient\_ID edges, including the NSCLC edge (\(0.8415\)). (E)~Spot counts on the directed Patient\_ID edges. (F)~Share of scanned neighbors whose cosine reaches the cutoff. The bars print the stored-export counts, \(2\) of \(17\) on CosMx NSCLC, \(0\) of \(8\) on CosMx CRC, \(8\) of \(15\) on CosMx HCC, and \(6\) of \(6\) on CosMx PDAC. Two of those denominators include a non-annotated placeholder column, one \texttt{NotDet} on hepatocellular and one \texttt{Undetermined} on PDAC, which is counted in both the numerator and the denominator of the PDAC bar. On eligible typed neighbours alone the fractions are \(2\) of \(17\), \(0\) of \(8\), \(8\) of \(14\), and \(5\) of \(5\). The stored colorectal export is separately truncated at eight rows, whereas the complete scan for that library holds sixteen columns of which fifteen carry an eligible annotated label. Bar colour is the library's reported call.
