Mean truth fraction for each of eight shared types sits on a builder floor of fifty reference cells per type. (A)~Mean truth fraction for each of eight shared types and each pair, with a builder floor of fifty reference cells per type. The horizontal axis is mean occupancy on \([0,1]\) (cell-count share). (B)~Wound-axis occupancy on Yerly BCC mixed spots. The horizontal axis is mean occupancy on \([0,1]\). Mean tumor truth is \(0.8425\) at Baseline and \(0.4665\) at Wound. (C)~MoMacDC mean truth fraction on the three computed pairs: \(0.0803\) (A from B), \(0.0129\) (B from A), and \(0.0636\) (D from C). The vertical axis is mean occupancy on \([0,1]\). (D)~Packed mixed-spot tumor-truth field for A from B. Point colour is tumor-truth fraction on \([0,1]\) (spot tumor occupancy), not gene expression and not a cell count. (E)~Packed mixed-spot tumor-truth field for B from A. Point colour is tumor-truth fraction on \([0,1]\) (spot tumor occupancy), not gene expression and not a cell count. (F)~Packed mixed-spot tumor-truth field for D from C. Point colour is tumor-truth fraction on \([0,1]\) (spot tumor occupancy), not gene expression and not a cell count.
