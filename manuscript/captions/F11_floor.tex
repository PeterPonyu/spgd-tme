Every reported type clears the builder floor of fifty reference cells, and occupancy on that supported simplex is concentrated in one column. Panels A--C read against the eight-type axis printed on A; the pair key colours the three computed transfers and the separate wound key colours the three conditions of D from C. (A)~Mean spot occupancy for each of the eight shared types on each computed pair. The horizontal axis is mean occupancy on \([0,1]\) (cell-count share), and every type on it is built from at least fifty reference cells. (B)~Share of spots that hold the type at all, that is carry a nonzero fraction of it, over the same eight types and the same three pairs. The malignant column is nonzero on \(90.9\%\) of B-from-A and \(85.3\%\) of D-from-C spots, whereas Pericyte is absent from \(95.5\%\) and \(87.2\%\) of those same spots: a mean near zero in A can be a type spread thinly over every spot or a type missing from most of them, and this panel is what separates the two. (C)~Wound-axis occupancy on Yerly BCC mixed spots by condition. The horizontal axis is mean occupancy on \([0,1]\). Mean tumor truth is \(0.8425\) at Baseline and \(0.4665\) at Wound. (D)~Packed mixed-spot tumor-truth field for A from B. Point colour is tumor-truth fraction on \([0,1]\) (spot tumor occupancy), not gene expression and not a cell count. (E)~Packed mixed-spot tumor-truth field for B from A, same colour meaning. (F)~Packed mixed-spot tumor-truth field for D from C, same colour meaning. The three donors pack to different footprints and are drawn in one shared view box so that D--F are the same size and sit under A--C; the added margin falls outside the tissue, each field keeps its own isotropic micrometre scale, and the printed bar is therefore still valid.
