Two locked operators compute mixed-spot TME composition and decide when the tumor column is reportable. Throughout the panel, blue marks the two operators this work adds, green marks a KEEP outcome and vermillion an ABSTAIN one, and steps inherited from the parent stack are drawn unfilled. (A)~Protocol path from a TME reference through specificity weights and a platform self-gate to the reportability gate, which writes ABSTAIN or a simplex composition. (B)~Locked operator identities with what each produces: gene specificity \(w_g=1-H(q_g)/\log K\), the weighted working signature \(S=e^{g\hat\gamma}(P\odot w)\), and simplex composition \(\hat\pi=\mathrm{simplex}(W^\star)\). (C)~KEEP/ABSTAIN gate: a malignant--neighbor cosine at or above the frozen cutoff \(c^\star=0.80\) returns ABSTAIN and renormalizes the remaining types; a cosine below the cutoff returns KEEP and reports a tumor fraction. (D)~Patient\_ID donor split: mixed spots and the reference are built from disjoint patients, so \(\{\text{spot patient}\}\cap\{\text{reference patient}\}=\emptyset\). (E)~Evaluation metrics recorded for a computed edge: overall RMSE, type-level Pearson correlation, spot-level Pearson correlation, tumor RMSE, and Jensen--Shannon divergence, with a \(B=1000\) interval on the \(t=0\) tumor RMSE. (F)~Six named operator steps in order with the wall-clock seconds each takes on one openST pass: signature extraction 0.86 s, specificity weights under 0.01 s, the weighted fit 8.37 s, the platform self-gate 47.96 s, the reportability gate 0.001 s, and the Poisson close 3.06 s. The self-gate is shaded because it carries 79.60\% of the pass, while the gate this work adds costs 0.001 s of it.
