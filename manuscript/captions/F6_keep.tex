The protocol reports CosMx tumor coordinates and places them beside locked tumor truth. (A)~Patient C cells in packed 0.51 mm fields of view, coloured by cancer membership on \([0,1]\) (1 = Cancer.cells). (B)~Locked tumor truth on packed Patient C fields. Point colour is the per-spot tumor truth fraction on \([0,1]\) (cell-count share of Cancer.cells). (C)~Estimated tumor fraction on packed Patient C fields. Point colour is the protocol tumor coordinate on \([0,1]\). On the full-\(n\) CosMx KEEP map at \(t=0\) (\(n=5686\)), estimated tumor fraction has median \(0.6739\) (quartiles \(0.0510\)--\(0.9195\); mean \(0.5428\)). (D)~One Patient C field of view coloured by cancer membership on \([0,1]\). (E)~Native malignant--neighbor cosine versus the frozen cutoff \(c^\star=0.80\) on openST, Xenium, Xenium FLEX, and CosMx. The vertical axis is cosine; KEEP is below the cutoff and ABSTAIN is at or above it. The CosMx cosine is \(0.603666\). (F)~Native KEEP and ABSTAIN library counts.
