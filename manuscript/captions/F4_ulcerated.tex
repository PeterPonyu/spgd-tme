Patient A is more stromal and Patient B is tumor-rich on the ulcerated-nodular pair. (A)~Patient A packed fields coloured by the eight shared types on native micrometre axes. (B)~Patient B packed fields coloured by cancer membership on \([0,1]\) (1 = Cancer.cells). (C)~One Patient A field of view coloured by type, with mixed-spot centroids as open circles. (D)~One Patient B field of view coloured by cancer membership on \([0,1]\). (E)~A-from-B mixed-spot tumor truth on packed Patient A fields. Point colour is the per-spot Cancer.cells truth fraction on \([0,1]\) (cell-count share). (F)~B-from-A mixed-spot tumor truth on packed Patient B fields. Point colour is the per-spot Cancer.cells truth fraction on \([0,1]\).
