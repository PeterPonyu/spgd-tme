Operator cost, measured on CPU with no accelerator anywhere in the protocol. Upper block: wall-clock seconds for one openST pass through signature extraction, specificity weights, the weighted fit, the platform self-gate, the reportability gate, and the Poisson close, with each step's share of the \(60.252\) s pass. The platform self-gate takes \(79.60\%\) of the pass and the reportability gate that decides KEEP or ABSTAIN takes \(0.001\) s, under \(0.01\%\) of it. Lower block: full-\(n\) \(t=0\) passes on each timed substrate, with spot count, wall-clock seconds, and the implied milliseconds per spot. The repeated 400-spot openST build over five warm runs is \(22.565\pm2.861\) s (CV \(12.68\%\), range \(19.026\)--\(25.588\) s) on the same CPU-only hardware (Intel Core Ultra 9 275HX, 24 CPUs, 62\,GiB RAM; Python 3.13).
