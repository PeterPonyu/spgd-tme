Four-patient CosMx basal-cell carcinoma is the mixed-spot TME object. (A)~Spot counts on the three computed pairs: A from B (\(1194\)), B from A (\(2020\)), and D from C (\(3261\)). The vertical axis is spots. (B)~Mean type mass of the eight shared types on A from B, B from A, and D from C. Tile fill is mean occupancy on \([0,1]\) (cell-count share). (C)~Locked malignant--neighbor cosine versus the frozen cutoff \(c^\star=0.80\) on openST, Xenium, Xenium FLEX, and CosMx. The vertical axis is cosine; bars below the cutoff are KEEP and bars at or above it are ABSTAIN. (D)~Directed Patient\_ID edges with available spot counts. The vertical axis is spots. (E)~Native KEEP and ABSTAIN library counts on openST, Xenium, Xenium FLEX, and CosMx. (F)~Approximate mixed-spot diameter on the three computed pairs (A from B \(45.7\,\mu\mathrm{m}\); B from A \(45.1\,\mu\mathrm{m}\); D from C \(42.6\,\mu\mathrm{m}\)).
