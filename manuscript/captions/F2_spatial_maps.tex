Four-patient CosMx basal-cell carcinoma is the mixed-spot TME object. (A)~Spot counts on the three computed pairs: A from B (\(1194\)), B from A (\(2020\)), and D from C (\(3261\)). The vertical axis is spots. (B)~Mean type mass of the eight shared types on A from B, B from A, and D from C. Tile fill and the printed value are mean occupancy on \([0,1]\) (cell-count share). (C)~Locked malignant--neighbor cosine versus the frozen cutoff \(c^\star=0.80\) on openST, Xenium, Xenium FLEX, and CosMx. The vertical axis is cosine; bars below the cutoff are KEEP and bars at or above it are ABSTAIN. (D)~Per-spot Cancer.cells truth fraction on the same three pairs. Violins are the full distribution, the inner box gives the quartiles and median, and \(n\) is the spot count. Medians are \(0.2500\) (A from B), \(0.9661\) (B from A), and \(0.8824\) (D from C). (E)~Share of a pair's spots on which each of the eight shared types is present at all, from \(4.5\%\) (Pericyte on B from A) to \(90.9\%\) (Cancer on B from A). (F)~Approximate mixed-spot diameter on the three computed pairs (A from B \(45.7\,\mu\mathrm{m}\); B from A \(45.1\,\mu\mathrm{m}\); D from C \(42.6\,\mu\mathrm{m}\)), with the median number of pooled cells per spot printed above each bar.
