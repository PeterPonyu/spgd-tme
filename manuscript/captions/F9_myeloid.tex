MoMacDC is an occupancy axis on the computed pairs. (A)~MoMacDC mean truth fraction on the three computed pairs. The vertical axis is mean occupancy on \([0,1]\) (cell-count share): \(0.0803\) on A from B, \(0.0129\) on B from A, and \(0.0636\) on D from C. (B)~A-from-B mixed-spot MoMacDC truth on packed Patient A fields. Point colour is the per-spot MoMacDC truth fraction on \([0,1]\). (C)~D-from-C mixed-spot MoMacDC truth on packed Patient D fields. Point colour is the per-spot MoMacDC truth fraction on \([0,1]\). (D)~Patient A myeloid cells in one field of view. Colour is cell-level MoMacDC occupancy on \([0,1]\) (1 = MoMacDC). (E)~Patient D wound-field myeloid cells. Colour is cell-level MoMacDC occupancy on \([0,1]\). (F)~A-from-B mixed-spot fibroblast truth on packed Patient A fields, not MoMacDC. Point colour is the per-spot fibroblast truth fraction on \([0,1]\). The 0--1 colour bar is type occupancy (truth fraction), not gene expression.
