# Extra-GPU queue (do not book)

`build_v4` is CPU numpy. This companion does **not** need a rented 4090.

| Item | Device | Status |
|---|---|---|
| Window 1 Q2–Q6 | local CPU | waiting for a sitting window |
| Window 2 `10` full-n t=0 ×3 | local CPU | after Window 1 |
| AutoDL / 4090 / vGPU | — | **do not add** |

When a later paper needs extra GPU, append a row here and wait for one wholesale approval. An empty queue is not a box to keep warm.
